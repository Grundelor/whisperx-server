# WhisperX Transcription Server

Docker-образ для транскрибации аудио через WhisperX + Pyannote.
Используется как серверная часть для Telegram-бота.

## Быстрый старт

Push в `main` → GitHub Actions автоматически собирает Docker image → `ghcr.io`
